import numpy as np
import torch
import torch.nn as nn
from torch.nn import functional as F

class CNN(nn.module):
    def __init__(self, batch_size, output_size, in_channels, out_channels, kernel_heights, stride, padding, dropout_probab, vocab_size, embedding_length, weights):
        pass





